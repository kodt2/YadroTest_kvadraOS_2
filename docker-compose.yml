version: '3.8'

services:
  monitor:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: monitor-app
    ports:
      - "8080:8080"
      - "8081:8081"
    restart: unless-stopped